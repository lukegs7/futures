create table ods.bar1m(
	instrument varchar(20),
	date varchar(19),
	open numeric,
	high numeric,
	low numeric,
	close numeric,
	volume numeric,
	amount numeric,
	open_intl numeric,
	update_time timestamp
	timestamp bigint
);

create table ods.bar5m(
	instrument varchar(20),
	date varchar(19),
	open numeric,
	high numeric,
	low numeric,
	close numeric,
	volume numeric,
	amount numeric,
	open_intl numeric,
	update_time timestamp
	timestamp bigint
);

create table ods.bar15m(
	instrument varchar(20),
	date varchar(19),
	open numeric,
	high numeric,
	low numeric,
	close numeric,
	volume numeric,
	amount numeric,
	open_intl numeric,
	update_time timestamp
	timestamp bigint
);

create table ods.bar30m(
	instrument varchar(20),
	date varchar(19),
	open numeric,
	high numeric,
	low numeric,
	close numeric,
	volume numeric,
	amount numeric,
	open_intl numeric,
	update_time timestamp
	timestamp bigint
);

create table ods.bar60m(
	instrument varchar(20),
	date varchar(19),
	open numeric,
	high numeric,
	low numeric,
	close numeric,
	volume numeric,
	amount numeric,
	open_intl numeric,
	update_time timestamp
	timestamp bigint
);

create table ods.bar1d(
	instrument varchar(20),
	date varchar(19),
	open numeric,
	high numeric,
	low numeric,
	close numeric,
	volume numeric,
	amount numeric,
	open_intl numeric,
	update_time timestamp
	timestamp bigint
);

